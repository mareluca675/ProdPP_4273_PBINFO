#include <iostream>

using namespace std;

int main()
{
    int n, p = 1, i = 1;
    
    cin >> n;
    
    while(i <= n)
    {   
        p = p * i * i;
        i ++;
    }
    
    cout << p;

    return 0;
}
